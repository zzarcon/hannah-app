<div class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to "dashboard" elementId="app-logo" class="navbar-brand"}}
          <img src="images/app-logo.png">
      {{/link-to}}
    </div>
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        {{#link-to "login" tagName="li"}}
          <a href="#">Login</a>
        {{/link-to}}
        {{#link-to "dashboard" tagName="li"}}
          <a href="#">Dashboard</a>
        {{/link-to}}
        {{#link-to "campaigns" tagName="li"}}
          <a href="#">Campaigns</a>
        {{/link-to}}
        {{#link-to "statistics" tagName="li"}}
          <a href="#">Statistics</a>
        {{/link-to}}
      </ul>
      <ul id="profile-info" class="nav navbar-nav navbar-right">
        <li>{{session.user.username}} <img {{bind-attr src="session.user.profilePicture"}}></li>
      </ul>
    </div>
  </div>
</div>
<div class="container">
  {{outlet}}
</div>